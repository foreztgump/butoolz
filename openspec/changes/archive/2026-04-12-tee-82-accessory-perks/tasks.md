## 1. Accessory Data & Types

- [x] 1.1 Create accessory type definitions and data file at `app/runes_dreaming/data/accessories.ts`. Define `AccessoryPerk` interface (`effect: string`, `runeColor: RuneType`, `requiredCount: number`) and `Accessory` interface (`id: string`, `name: string`, `type: "ring" | "necklace"`, `perks: AccessoryPerk[]`). Export `RINGS` and `NECKLACES` arrays with all game accessories. Import `RuneType` from a shared types location or re-export from page. **Acceptance:** File exports typed arrays, every accessory has at least one perk, all `runeColor` values are valid RuneType, all `requiredCount` values are positive integers. **Quality:** No magic values (use RuneType type), descriptive names, YAGNI (no extra fields beyond what's needed). **Error handling:** N/A — static data, validated by TypeScript compiler.

- [x] 1.2 Extract shared `RuneType` and `RUNE_TYPES` into `app/runes_dreaming/types.ts` so both `page.tsx` and `accessories.ts` can import them without circular dependencies. Update `page.tsx` imports accordingly. **Acceptance:** `page.tsx` still compiles and functions identically. Types are importable from the shared file. **Quality:** SRP (types in their own module), no duplication. **Error handling:** N/A — compile-time only.

## 2. Perk Activation Logic

- [x] 2.1 Create `app/runes_dreaming/lib/computePerkStatus.ts` — a pure function that takes `results` (rune counts) and a selected accessory, and returns perk activation status. For each perk: `{ effect, runeColor, requiredCount, currentCount, activated, deficit }`. **Acceptance:** Given a results object and accessory, correctly computes activated/inactive status and deficit for all perks. Rainbow runes and mount bonus are already counted in `results` — no special handling needed. **Quality:** Single responsibility (compute perk status only), max 3 params (use options object if needed), Law of Demeter (access results directly, don't chain), descriptive names (`deficit` not `diff`). **Error handling:** Return empty arrays for null/undefined accessory IDs. **Test:** Create `app/runes_dreaming/lib/computePerkStatus.test.ts` with AAA tests covering: activated perk, inactive perk with deficit, no accessory selected, all perks activated.

## 3. Accessory Selector & Display Component

- [x] 3.1 Create `app/runes_dreaming/components/AccessoryPerks.tsx` — the main component receiving `results` as its only prop. Internally manages selected ring/necklace state via `useState`. Renders two Select dropdowns (ring, necklace) with "None" default option. Uses `computePerkStatus` to compute and display perk status. **Acceptance:** Dropdowns list all rings and necklaces by name. Selecting an accessory shows its perks with activation status. Activated perks show green checkmark + effect text + rune color badge. Inactive perks show dimmed state + "X more [color] runes needed". No accessory selected shows prompt message. **Quality:** Component ≤40 lines logic (JSX excluded), ≤3 nesting levels, uses existing shadcn/ui components (Select, Card, Badge), no magic values, descriptive names. **Error handling:** N/A — all data is static, selections are controlled.

## 4. Integration & Persistence

- [x] 4.1 Import `AccessoryPerks` into `page.tsx` and render it below the Rune Distribution section inside the Configuration tab. Pass `results` as prop. **Acceptance:** Section appears below distribution, updates reactively when rune configuration changes. **Quality:** Single line of integration (`<AccessoryPerks results={results} />`), no prop drilling beyond results. **Error handling:** N/A.

- [x] 4.2 Add localStorage persistence for selected accessories. Save selected ring/necklace IDs when "Save Config" is clicked, restore them on "Load Config". Maintain backward compatibility — old configs without accessory data load with "None" selections. **Acceptance:** Save/load round-trips accessory selections. Old configs still load without errors. Page reload preserves selections. **Quality:** Follow existing save/load pattern from mount bonus (TEE-81), no duplication of persistence logic. **Error handling:** Graceful fallback to "None" for invalid/missing accessory IDs in saved config.
